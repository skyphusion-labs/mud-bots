extensions:
  - addsTo:
      pack: codeql/javascript-all
      extensible: barrierModel
    data:
      - ["global", "Member[resolveWorldKey].ReturnValue", "request-forgery", "manual"]
      - ["global", "Member[worldWsUrl].ReturnValue", "request-forgery", "manual"]
      - ["global", "Member[redactForLog].ReturnValue", "clear-text-logging", "manual"]
