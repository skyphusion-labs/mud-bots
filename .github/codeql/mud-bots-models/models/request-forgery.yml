extensions:
  - addsTo:
      pack: codeql/javascript-all
      extensible: barrierModel
    data:
      - ["global", "Member[resolveWorldKey].ReturnValue", "request-forgery"]
      - ["global", "Member[worldWsUrl].ReturnValue", "request-forgery"]
